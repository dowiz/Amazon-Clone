pipeline {
    agent any
    environment {
        DB_PASSWORD = credentials('MSSQL-PASSWORD')
    }
    stages {
        stage('Build and Deploy') {
            steps {
                script {
                    sh '''
                        cd ./ToolBox/
                        docker-compose up -d
                        '''
                }
            }
        }
    }
}
