#!groovy
//  groovy Jenkinsfile
pipeline  {
    agent any;
    stages {
        
         stage("Hostname")
         {
             steps{
                sh """
                #!/bin/bash
                hostname
                """
             }
         }
    }
}
